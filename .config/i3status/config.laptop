
order += "battery all"
order += "time"

battery all {
	last_full_capacity = true
	low_threshold = 20
	threshold_type = "percentage"
	format = "Battery: %status %percentage %remaining" 
}
time {
	format = "%H:%M %d %b "
}
